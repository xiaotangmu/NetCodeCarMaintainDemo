namespace Interface.System
{
    public interface IRoomRepository : IBaseRepository
    {
        //Task<IEnumerable<T_ROOM>> GetGroupWithPaging(string floorCode, int pageIndex, int pageSize);

        //Task<IEnumerable<T_ROOM>> GetGroup(T_ROOM searchCondition);

        //Task<IEnumerable<PreallocationRoomInfo>> GetVacantGroup(RoomType type);

        //Task<int> Count(string floorCode);

        //Task<string> AddAsync(T_ROOM room, IDbTransaction transaction = null);

        //Task<bool> EditAsync(Dictionary<string, object> setting, Dictionary<string, object> where, IDbTransaction transaction = null);

        //Task<bool> DeleteAsync(string index, IDbTransaction transaction = null);

        //Task<int> GetCountAsync(HostelListSearchViewModel searchModel);

        //Task<IEnumerable<HostelInfoViewModel>> GetGroupWithPaging(HostelListSearchViewModel searchModel);

        //Task<bool> IsExist(string roomIndex);
    }
}
